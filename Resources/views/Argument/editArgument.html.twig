{% block content %}
    <h1>Edit Argument Form</h1>
    {%  if (form is not null) %}
    <form action="{{ url('api_1_post_argument') }}" method="PUT" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <input type="submit" value="submit">
    </form>
    {% endif %}
{% endblock %}
